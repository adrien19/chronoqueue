# Dockerfiles

This includes dockerfiles to build ChronoQueue release and development container images for go dev environment.

* Dockerfile: ChronoQueue Release Image
* Dockerfile-dev: Development container image for VS Code Remote-Containers and GitHub Codespaces.

